import 'package:meta/meta.dart';

/// An annotation to indicate that an HTTP request should not have a request body.
@immutable
class NoBody {
  /// Creates an instance of [NoBody].
  const NoBody();
}
