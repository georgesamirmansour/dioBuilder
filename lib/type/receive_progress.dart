import 'package:meta/meta.dart';

/// A class representing progress information for receiving data.
@immutable
class ReceiveProgress {
  /// Creates an instance of [ReceiveProgress].
  const ReceiveProgress();
}
