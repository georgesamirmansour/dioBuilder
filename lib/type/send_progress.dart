import 'package:meta/meta.dart';

/// A class representing progress information for sending data.
@immutable
class SendProgress {
  /// Creates an instance of [SendProgress].
  const SendProgress();
}
